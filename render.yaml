services:
  - type: web
    name: qasmart-dashboard
    env: node
    plan: starter
    region: oregon
    buildCommand: npm ci && npm run render-build
    startCommand: npm run render-start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /healthz
